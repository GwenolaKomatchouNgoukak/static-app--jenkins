pipeline{
    stages{
        stage('CodeScan'){
            steps{
                sh 'touch file.txt'
                sh 'echo "It works! ">file.txt'
                sh 'cat file.txt'
            }

        }
    }
}